{erl_opts, [{ parse_transform, lager_transform },
            fail_on_warning, warn_export_all]}.

{deps, [{ lager, ".*", { git, "https://github.com/basho/lager.git", { tag, "2.1.0" }}}]}.

{deps_dir, "deps"}.
{sub_dirs, ["rel"]}.

{xref_checks, [undefined_function_calls]}.
{eunit_opts, [verbose, no_tty]}.
{eunit_compile_opts, [{i, "../"}]}.
